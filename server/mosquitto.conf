# YogiBot 개발용 MQTT 브로커 설정 (Mac 호스트에서 실행)
#
#   mosquitto -c server/mosquitto.conf -v
#
# 우분투 VM(다른 호스트)에서 접속해야 하므로 모든 인터페이스에서 listen 한다.
# ⚠️ 개발/내부망 전용. 운용 시에는 인증/TLS 필수 (명세서 6. rosbridge 보안 참고).

listener 1883 0.0.0.0
allow_anonymous true

# 연결 로그
log_type all
connection_messages true
